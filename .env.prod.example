# Example production environment file (do NOT commit real secrets)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=strong_password_here
POSTGRES_DB=app_db
# DATABASE_URL should point to the production DB; use the format below or override using secret management
DATABASE_URL=postgres://postgres:strong_password_here@db:5432/app_db
NODE_ENV=production
PORT=3000
